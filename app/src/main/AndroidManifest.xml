<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.biluutech.medicineprescription">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Doctor.DoctorRecentReviewsActivity" android:parentActivityName=".Activities.Patient.PatientHomeActivity"></activity>
        <activity
            android:name=".Activities.Doctor.DoctorReviewActivity"
            android:parentActivityName=".Activities.Patient.PatientHomeActivity" />
        <activity
            android:name=".Activities.Patient.AllergiesActivity"
            android:parentActivityName=".Activities.Patient.PatientHomeActivity" />
        <activity android:name=".Activities.Patient.PatientSearchActivity" />
        <activity android:name=".Activities.Doctor.DoctorAppointmentsActivity" />
        <activity android:name=".Activities.Doctor.DoctorProfileActivity" />
        <activity android:name=".Activities.Doctor.DoctorHomeActivity" />
        <activity android:name=".Activities.Patient.AppointmentActivity" />
        <activity android:name=".Activities.Admin.AdminHomeActivity" />
        <activity
            android:name=".Activities.Patient.PatientProfileActivity"
            android:parentActivityName=".Activities.Patient.PatientHomeActivity" />
        <activity android:name=".Activities.Patient.PatientHomeActivity" />
        <activity android:name=".Activities.Admin.AdminLoginActivity" />
        <activity android:name=".Activities.Doctor.DoctorLoginActivity" />
        <activity android:name=".Activities.Doctor.DoctorRegisterActivity" />
        <activity android:name=".Activities.Patient.PatientRegisterActivity" />
        <activity android:name=".Activities.Patient.PatientLoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>